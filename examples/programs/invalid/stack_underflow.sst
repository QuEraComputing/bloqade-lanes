sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// arch: simple
// expect: StackUnderflow
// Invalid: dup on empty stack

fn @main() {
  cpu::cpu.dup
  cpu::cpu.halt
}
.text(root).
.section(root).
