sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// Invalid: initial_fill expects location, but stack has float

fn @main() {
  cpu.const_float 1.0
  lanes.initial_fill 1
  cpu.halt
}
.text(root).
.section(root).
