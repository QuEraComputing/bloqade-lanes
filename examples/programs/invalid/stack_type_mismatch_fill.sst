sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// arch: simple
// expect: TypeMismatch
// Invalid: initial_fill expects location, but stack has float

fn @main() {
  cpu::cpu.const f64, 1.0
  lanes::lanes.initial_fill 1
  cpu::cpu.halt
}
.text(root).
.section(root).
