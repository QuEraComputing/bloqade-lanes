sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// Invalid: bus_id=20 does not exist (site buses 0-8 only)

fn @main() {
  lanes::lanes.const_lane 0x0000001400000000    // fwd, site_bus, word 0, site 0, bus 20
  cpu::cpu.halt
}
.text(root).
.section(root).
