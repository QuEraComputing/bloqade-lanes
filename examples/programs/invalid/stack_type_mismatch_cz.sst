sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// Invalid: cz expects zone, but stack has float

fn @main() {
  cpu.const_float 1.0
  lanes.cz
  cpu.halt
}
.text(root).
.section(root).
