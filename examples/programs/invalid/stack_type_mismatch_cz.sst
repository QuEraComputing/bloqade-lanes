sst v1

.section(root):
.header(root):
version 1.0
.header(root).
.text(root):
// Invalid: cz expects zone, but stack has float

fn @main() {
  cpu::cpu.const f64, 1.0
  lanes::lanes.cz
  cpu::cpu.halt
}
.text(root).
.section(root).
